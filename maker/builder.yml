paths:
  package: "WebApp"

defaults:
  agents:
    geo: "co"
    arch: "amd"
    cpu: "64"
    os: "linux"

steps:
  - name: "Makerverse"
    include:
      paths: [ "Server/*", "WebApp/*", "DesktopApp/*" ] # Source code for frontend or backend.
    build: "./maker/mv.build.yml"
    deploy: "./maker/mv.deploy.yml"
