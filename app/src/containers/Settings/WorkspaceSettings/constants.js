import constants from 'namespace-constants';

export const { MODAL_CREATE_RECORD, MODAL_UPDATE_RECORD } = constants('containers/settings/workspaces', [
  'MODAL_CREATE_RECORD',
  'MODAL_UPDATE_RECORD',
]);
