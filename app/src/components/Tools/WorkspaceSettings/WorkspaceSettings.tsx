import * as React from 'react';
import { Grid } from '@material-ui/core';
import {ToolBase} from '../types';

const WorkspaceSettings: ToolBase = (props) => {
  return (
    <Grid item xs={12}>
      Console component
    </Grid>
  );
};

export default WorkspaceSettings;
